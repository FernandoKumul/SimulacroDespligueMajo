name: Produccion

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Ejecutar
      run: npm run start

    - name: Despliegue
      run: |
        echo "Ejecutando Despliegue.."
        echo "Despliegue exitoso"

    - name: Monitoreo
      run:
        echo "Ejecutando métricas de monitoreo..."